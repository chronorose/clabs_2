#include <stdio.h>
#include <assert.h>

void render() {
    
}

//settings; walls; randomize(?); several players; ncurses moment; 
int main() {
    long x = 0;
    scanf("%ld", &x);
    assert(x >= 2);
}
